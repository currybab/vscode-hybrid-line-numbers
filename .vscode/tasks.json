{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "command": "pnpm",
            "args": ["run", "build"],
            "label": "pnpm: build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
